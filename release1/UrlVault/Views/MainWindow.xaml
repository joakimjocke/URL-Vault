<Window x:Class="UrlVault.Views.MainWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:UrlVault.Converters"
        Title="UrlVault - URL Manager" Height="700" Width="1100"
        MinHeight="500" MinWidth="900"
        Background="#F5F5F5">
    <Window.Resources>
        <converters:StringJoinConverter x:Key="StringJoinConverter"/>
        <converters:CategoryToColorConverter x:Key="CategoryToColorConverter"/>
        <converters:CategoryToLeafNameConverter x:Key="CategoryToLeafNameConverter"/>
        <converters:HexColorToBrushConverter x:Key="HexColorToBrushConverter"/>
        <converters:DateSavedConverter x:Key="DateSavedConverter"/>

        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Background" Value="#3F51B5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5C6BC0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#9E9E9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Background" Value="#E53935"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EF5350"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#9E9E9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1A237E" Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="UrlVault" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="+ Add" Command="{Binding AddCommand}" Style="{StaticResource ToolbarButtonStyle}"/>
                    <Button Content="Edit" Command="{Binding EditCommand}" Style="{StaticResource ToolbarButtonStyle}"/>
                    <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButtonStyle}"/>
                    <Button Content="Copy URL" Command="{Binding CopyUrlCommand}" Style="{StaticResource ToolbarButtonStyle}"/>
                    <Button Content="Open in Browser" Command="{Binding OpenUrlCommand}" Style="{StaticResource ToolbarButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#E8EAF6" Padding="16,8">
            <Border Background="White" CornerRadius="4" BorderBrush="#C5CAE9" BorderThickness="1">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         BorderThickness="0" Padding="8,6" FontSize="13"
                         Background="Transparent"
                         VerticalAlignment="Center"/>
            </Border>
        </Border>

        <Border Grid.Row="2" Background="#F3F4FF" Padding="16,6" BorderBrush="#C5CAE9" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Tags:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold" Foreground="#3F51B5"/>
                <ItemsControl ItemsSource="{Binding Config.Tags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding}" Margin="0,0,12,0" VerticalAlignment="Center"
                                      Checked="TagFilter_Changed" Unchecked="TagFilter_Changed"
                                      Tag="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <Grid Grid.Row="3" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" BorderBrush="#C5CAE9" BorderThickness="1" CornerRadius="4" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TreeView x:Name="CategoryTreeView"
                              ItemsSource="{Binding CategoryTree}"
                              SelectedItemChanged="CategoryTreeView_SelectedItemChanged"
                              AllowDrop="True"
                              PreviewMouseLeftButtonDown="CategoryTreeView_PreviewMouseLeftButtonDown"
                              PreviewMouseMove="CategoryTreeView_PreviewMouseMove"
                              DragOver="CategoryTreeView_DragOver"
                              DragLeave="CategoryTreeView_DragLeave"
                              Drop="CategoryTreeView_Drop"
                              BorderThickness="0"
                              Background="Transparent">
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=TreeView}}"/>
                                <Setter Property="ContextMenu">
                                    <Setter.Value>
                                        <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                            <MenuItem Header="Rename"
                                                      Command="{Binding PlacementTarget.Tag.RenameCategoryCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding}"/>
                                            <MenuItem Header="Add Subcategory"
                                                      Command="{Binding PlacementTarget.Tag.AddSubcategoryCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding}">
                                                <MenuItem.Style>
                                                    <Style TargetType="MenuItem">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsGroup}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </MenuItem.Style>
                                            </MenuItem>
                                            <MenuItem Header="Delete"
                                                      Command="{Binding PlacementTarget.Tag.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding}">
                                                <MenuItem.Style>
                                                    <Style TargetType="MenuItem">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsAll}" Value="True">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </MenuItem.Style>
                                            </MenuItem>
                                        </ContextMenu>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TreeView.ItemContainerStyle>
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <TextBlock>
                                    <Run Text="{Binding Name}">
                                        <Run.Style>
                                            <Style TargetType="Run">
                                                <Setter Property="Foreground" Value="#263238"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsGroup}" Value="True">
                                                        <Setter Property="Foreground" Value="{Binding Color, Converter={StaticResource HexColorToBrushConverter}}"/>
                                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Run.Style>
                                    </Run>
                                    <Run Text=" "/>
                                    <Run Text="{Binding Count, StringFormat='({0})'}" Foreground="#607D8B"/>
                                </TextBlock>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>

                    <Button Grid.Row="1"
                            Margin="0,8,0,0"
                            Content="+ New Category"
                            Command="{Binding NewCategoryCommand}"
                            Style="{StaticResource ToolbarButtonStyle}"/>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="White" BorderBrush="#C5CAE9" BorderThickness="1" CornerRadius="4">
                <ListView x:Name="UrlListView"
                          ItemsSource="{Binding EntriesView}"
                          SelectedItem="{Binding SelectedEntry}"
                          BorderThickness="0"
                          FontSize="13">
                    <ListView.View>
                        <GridView x:Name="UrlGridView" AllowsColumnReorder="True">
                            <GridViewColumn Header="Title" Width="250" DisplayMemberBinding="{Binding Title}"/>
                            <GridViewColumn Header="URL" Width="280" DisplayMemberBinding="{Binding Url}"/>
                            <GridViewColumn Header="Category" Width="140">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Padding="6,2"
                                                CornerRadius="8"
                                                Background="{Binding Category, Converter={StaticResource CategoryToColorConverter}, ConverterParameter={x:Reference RootWindow}}"
                                                HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding Category, Converter={StaticResource CategoryToLeafNameConverter}}"
                                                       Foreground="White"
                                                       FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </Border>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Comment" Width="220">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Comment}" TextTrimming="CharacterEllipsis"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Tags" Width="160">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Tags, Converter={StaticResource StringJoinConverter}}"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Date Saved" Width="140">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DateSaved, Converter={StaticResource DateSavedConverter}}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                    <ListView.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding OpenUrlCommand}"/>
                    </ListView.InputBindings>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="4,6"/>
                            <!-- Tag carries the MainViewModel so the ContextMenu can reach its commands -->
                            <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                            <Setter Property="ContextMenu">
                                <Setter.Value>
                                    <ContextMenu>
                                        <MenuItem Header="Edit"
                                                  Command="{Binding PlacementTarget.Tag.EditCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <MenuItem Header="Open in Browser"
                                                  Command="{Binding PlacementTarget.Tag.OpenUrlCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <MenuItem Header="Copy URL"
                                                  Command="{Binding PlacementTarget.Tag.CopyUrlCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <Separator/>
                                        <MenuItem Header="Delete"
                                                  Command="{Binding PlacementTarget.Tag.DeleteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  Foreground="#E53935"/>
                                    </ContextMenu>
                                </Setter.Value>
                            </Setter>
                            <EventSetter Event="PreviewMouseRightButtonDown" Handler="UrlListViewItem_PreviewMouseRightButtonDown"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E8EAF6"/>
                                    <Setter Property="Foreground" Value="#1A237E"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F3F4FF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Border>
        </Grid>

        <Border Grid.Row="4" Background="#1A237E" Padding="16,5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Foreground="White" FontSize="12">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="Showing {0} entries">
                            <Binding Path="DisplayedCount"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock Text=" | " Foreground="#9FA8DA" FontSize="12"/>
                <TextBlock Text="UrlVault" Foreground="#9FA8DA" FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
